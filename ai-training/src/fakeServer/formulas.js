import data from './db.json'

const formulas = {
  // write covariance formula
  'covFunc': (X, Y) => {

  },
  // covariance matrix formula
  'covMat': (X) => {

  },
  // eigenvalues and vectors
  'eigen': (X) => {

  },
  // projection matrix for future use?
  'projection': (X) => {

  }
}


// write covariance matrix formula
export default formulas